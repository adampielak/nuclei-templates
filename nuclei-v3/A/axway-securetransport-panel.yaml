http:
- extractors:
  - group: 1
    part: body
    regex:
    - '"SecureTransport", "([0-9.]+)",'
    type: regex
  host-redirects: true
  matchers:
  - condition: or
    part: body
    type: word
    words:
    - <title>Axway SecureTransport
    - alt="Axway SecureTransport"
    - Welcome to SecureTransport
    - PrintServerInfo("SecureTransport
  - status:
    - 200
    type: status
  matchers-condition: and
  max-redirects: 2
  method: GET
  path:
  - '{{BaseURL}}'
id: axway-securetransport-panel
info:
  author: righettod
  classification:
    cpe: cpe:2.3:a:axway:securetransport:*:*:*:*:*:*:*:*
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
  description: 'AXWAY SecureTransport login panel was detected.

    '
  metadata:
    fofa-query:
    - title="securetransport" || http.favicon.hash:1330269434
    - title="st web client"
    google-query:
    - intitle:"st web client"
    - intitle:"securetransport" || http.favicon.hash:1330269434
    max-request: 1
    product: securetransport
    runzero-match: any(each(service["html.titles"]), {# matches "(?i)securetransport"})
      || any(each(service["html.titles"]), {# matches "(?i)st web client"})
    shodan-query:
    - http.title:"SecureTransport" || http.favicon.hash:1330269434
    - http.title:"st web client"
    - http.title:"securetransport" || http.favicon.hash:1330269434
    vendor: axway
    verified: true
  name: Axway SecureTransport Login Panel - Detect
  reference:
  - https://www.axway.com/en/products/managed-file-transfer/securetransport
  severity: info
  tags: axway,discovery,panel,securetransport
