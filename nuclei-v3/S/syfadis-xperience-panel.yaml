id: syfadis-xperience-panel

info:
  name: Syfadis Xperience Login Panel - Detect
  author: righettod
  severity: info
  description: |
    Syfadis Xperience login panel was detected.
  reference:
    - https://syfadis.fr/xperience
  metadata:
    runzero-match: any(each(service["html.titles"]), {# matches "(?i)Syfadis Xperience"})
    verified: true
    max-request: 1
    fofa-query: title="Syfadis Xperience"
  tags: panel,syfadis,login,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/Directory/Login/Login.aspx"

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_any(to_lower(body), "syfadis xperience", "syfadis.supervision.browsersupport") && contains(to_lower(body), "loginpage")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - 'ajaxz/([0-9.]+)/Common'

