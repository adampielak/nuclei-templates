http:
- extractors:
  - group: 1
    internal: true
    name: version
    part: body
    regex:
    - '(?mi)Stable tag: ([0-9.]+)'
    type: regex
  - group: 1
    name: version
    part: body
    regex:
    - '(?mi)Stable tag: ([0-9.]+)'
    type: regex
  matchers:
  - status:
    - 200
    type: status
  - part: body
    type: word
    words:
    - ripe-hd-player
  - dsl:
    - compare_versions(version, '<= 1.1')
    type: dsl
  matchers-condition: and
  max-redirects: 3
  method: GET
  path:
  - '{{BaseURL}}/wp-content/plugins/ripe-hd-player/readme.txt'
  redirects: true
id: ripe-hd-player-1ee8086b6ee587d5f0c2a37f98b0dcae
info:
  author: topscoder
  classification:
    cve-id: null
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H
    cvss-score: 9.8
  description: 'The Ripe HD FLV plugin for WordPress is vulnerable to generic SQL
    Injection via the ''id'' parameter in the ''config.php'' file in versions up to,
    and including, 1.1 due to insufficient escaping on the user-supplied parameter
    and lack of sufficient preparation on the existing SQL query.  This makes it possible
    for unauthenticated attackers to append additional SQL queries into already existing
    queries that can be used to extract sensitive information from the database.

    '
  metadata:
    fofa-query: wp-content/plugins/ripe-hd-player/
    google-query: inurl:"/wp-content/plugins/ripe-hd-player/"
    shodan-query: 'vuln:'
  name: 'Ripe HD FLV <= 1.1 - SQL Injection

    '
  reference:
  - https://github.com/topscoder/nuclei-wordfence-cve
  - https://www.wordfence.com/threat-intel/vulnerabilities/id/cf159a11-9490-4f79-a62d-c279cfe26108?source=api-prod
  severity: critical
  tags: cve,wordpress,wp-plugin,ripe-hd-player,critical
