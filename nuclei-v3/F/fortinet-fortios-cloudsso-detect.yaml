id: fortinet-fortios-cloudsso-detect

info:
  name: Fortinet FortiOS FortiCloud SSO - Detect
  severity: info
  author: darses
  description: |
    Fortinet FortiOS FortiCloud SSO function detected.
  metadata:
    verified: false
    max-requests: 1
    vendor: fortinet
    product: fortios
  tags: fortinet,fortios,detect,sso

http:
  - method: GET
    path:
      - "{{BaseURL}}/saml/login-forticloud?host={{Hostname}}"

    matchers-condition: and
    matchers:
      - type: word
        words:
          - "fortinet.com/saml-idp"
          - "SAMLRequest"

      - type: status
        status:
          - 200
