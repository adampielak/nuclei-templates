id: windows-admin-center-panel

info:
  name: Windows Admin Center Panel - Detection
  author: darses
  severity: info
  description: |
    Detect Windows Admin Center Panel web interface.
  reference:
    - https://github.com/microsoft/Windows-admin-center
  metadata:
    runzero-match: service["favicon.ico.image.mmh3"] == "-765377534" || any(each(service["html.titles"]), {# matches "(?i)Windows Admin Center"})
    shodan-query:
      - http.favicon.hash:-765377534
      - title:"Windows Admin Center"
    fofa-query:
      - icon_hash="-765377534"
      - title="Windows Admin Center"
  tags: detect,windows,panel,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: status
        status:
          - 403

      - type: word
        part: body
        words:
          - "Windows Admin Center</title>"

